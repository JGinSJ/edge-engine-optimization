# OS
.DS_Store

# Node
node_modules/

# Python
__pycache__/
*.pyc

# Rust / Spin build output
target/
.spin/

# EdgeWorker bundle artifact
bundle.tgz
**/bundle.tgz

# Local secrets / env — never commit
.env
.env.local
**/.claude/settings.local.json

# Apple refurb stock checker — local config (holds credentials) and runtime state
apple-refurb-stock-checker/config.json
apple-refurb-stock-checker/state.json
apple-refurb-stock-checker/*.log
