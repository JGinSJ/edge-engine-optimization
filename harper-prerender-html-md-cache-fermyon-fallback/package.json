{
  "name": "harper-prerender-backend",
  "version": "1.0.0",
  "description": "Prerender backend — headless-Chrome renderer + Harper component (HTML+Markdown render cache) for edge-engine-optimization",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test harper-prerender/src/util/markdown.test.js"
  }
}
